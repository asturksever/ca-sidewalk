[project]
name = "ca-sidewalk-pipeline"
version = "0.1.0"
description = "Data pipeline for the Canadian Sidewalk Map (CPND x OSM conflation)"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.1",
    "pyogrio>=0.10",
    "shapely>=2.0",
    "pyarrow>=17",
    "geopandas>=1.0",
    "numpy>=1.26",
    "quackosm>=0.12",
]
